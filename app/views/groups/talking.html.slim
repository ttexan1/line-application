.group.index
  .top-bar トーク一覧
  .boxs
    - @groups.each do |group|
      .box
        .mask = link_to "", group_messages_path(group)
        .image = image_tag(group.room(current_user).thumbnail, size: "60x60", alt: "") if group.room(current_user).thumbnail
        .contents
          .name = group.room(current_user).name
          .last_msg = group.messages.last.content if group.messages.last
